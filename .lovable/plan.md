

# Delete `public/favicon.svg`

## What
Remove the unused `public/favicon.svg` file. The project already uses `public/favicon.png` (referenced in `index.html`), so this SVG is dead weight.

## Steps
1. Delete `public/favicon.svg`

No other files reference this SVG, so no additional changes needed.

